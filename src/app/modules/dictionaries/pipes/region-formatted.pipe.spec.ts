import { RegionFormattedPipe } from './region-formatted.pipe';

describe('RegionFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
