import { PublicRoadsFormattedPipe } from './public-roads-formatted.pipe';

describe('PublicRoadsFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicRoadsFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
