import {RoadImportanceFormattedPipe} from './road-importance-formatted.pipe';

describe('RoadImportanceFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new RoadImportanceFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
