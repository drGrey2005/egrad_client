import {OktmoValuesFormattedPipe} from './oktmo-values-formatted.pipe';

describe('OktmoValuesFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new OktmoValuesFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
